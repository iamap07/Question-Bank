'use client'
import { Database, ShieldCheck, HardDrive } from 'lucide-react'
import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { useQuestions } from '@/hooks/use-question-store'
export default function Settings(){const {questions}=useQuestions();const supabaseReady=Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);return <div className="mx-auto max-w-4xl space-y-6"><div><h1 className="text-2xl font-semibold">Settings</h1><p className="mt-1 text-sm text-slate-500">Environment and storage configuration.</p></div><Card><CardHeader><CardTitle>Storage</CardTitle></CardHeader><CardContent className="space-y-4"><div className="flex items-center justify-between rounded-lg border border-slate-200 p-4 dark:border-slate-800"><div className="flex items-center gap-3"><HardDrive size={18}/><div><div className="font-medium">Browser / Indexed local mode</div><div className="text-xs text-slate-500">Current fallback used for instant testing.</div></div></div><Badge variant="success">Active · {questions.length.toLocaleString()} records</Badge></div><div className="flex items-center justify-between rounded-lg border border-slate-200 p-4 dark:border-slate-800"><div className="flex items-center gap-3"><Database size={18}/><div><div className="font-medium">Supabase / PostgreSQL</div><div className="text-xs text-slate-500">Configure env variables and run the included schema to enable production persistence.</div></div></div><Badge variant={supabaseReady?'success':'muted'}>{supabaseReady?'Configured':'Not configured'}</Badge></div></CardContent></Card><Card><CardHeader><CardTitle>Security</CardTitle></CardHeader><CardContent><div className="flex items-start gap-3 text-sm"><ShieldCheck className="mt-0.5 text-green-600" size={18}/><div><div className="font-medium">Safe HTML rendering</div><p className="mt-1 text-slate-500">Question HTML is sanitized before display; service-role secrets are never expected in client code.</p></div></div></CardContent></Card></div>}
